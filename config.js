/* Supabase connection. Safe to be public — the anon key is meant for the browser,
   and row-level security controls access. Fill these in from your Supabase project
   (Project Settings → API). Leave blank to run on the built-in verified dataset. */
window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
